@model OnlineShoppingCart_MVC.Models.Admin

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Dashboard</title>
</head>
<body>

    <div>
        <h4>Admin</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.admin_name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.admin_name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.admin_password)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.admin_password)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model?.admin_id">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
</body>
</html>
